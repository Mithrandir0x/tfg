#!/bin/sh

### BEGIN INIT INFO
# Provides:          jetty
# Required-Start:    $local_fs $network $named $time $syslog
# Required-Stop:     $local_fs $network $named $time $syslog
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
### END INIT INFO

. /etc/profile.d/jetty.sh
. /etc/profile.d/java.sh
<%= scope.lookupvar('beabloo_log_gateway::JETTY_DEFAULT_DIR') %>/bin/jetty.sh "$@"